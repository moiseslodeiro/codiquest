
<!-- File created automatically by Codiquest Converter, do not modify because changes may be lost -->
<script>
  import Alert from '$lib/components/Alert.svelte';
  import Code from '$lib/components/Code.svelte';
  import Message from '$lib/components/Message.svelte';
  import ModalImage from '$lib/components/ModalImage.svelte';
  import Page from '$lib/templates/Page.svelte';
  import File from '$lib/components/File.svelte';
  import Browser from '$lib/components/Browser.svelte';
  import { base } from '$app/paths';
</script><Page><p><ModalImage src="img/codi/codi.webp" alignment="left" width="" alt="Codi" modal="false" /> Lo más importante ya ha pasado: ya sabes cómo crear módulos manualmente, entiendes cómo funcionan los componentes e, incluso, ¡sabes cómo automatizar su creación! ¿Qué te parece si ahora <a href="{base}/docs/personalizando-codiquest" class="link link-primary internal">personalizamos un poco la página</a>? Será un módulo muy breve que nos dirá dónde cambiar el logotipo y el título, y dónde modificar la sección de <code>Acerca de</code> para que puedas usar la plataforma con tu propia imagen.</p>
</Page>
<style>
ul,
ol {
  padding-left: 1.5rem;
  margin-left: 0;
  margin-bottom: 10px;
}

ul > li,
ol > li {
  #list-style-position: inside;
}

ol > li {
  list-style-type: decimal-leading-zero;
}

ul {
  list-style-type: disc;
  margin-bottom: 10px;
}

ul ul {
  padding-left: 1.25rem;
  list-style-type: circle;
}

ul ul ul {
  padding-left: 1.25rem;
  list-style-type: square;
}

ul ul ul ul {
  padding-left: 1.25rem;
  list-style-type: disc;
}

hr {
  margin: 10px 0 10px 0;
  clear: both;
}

code {
  font-family: 'Courier New', Courier, monospace;
  font-size: 0.9rem;
  background-color: #f5f5f5;
  padding: 0.2rem 0.4rem;
  border-radius: 4px;}
</style>