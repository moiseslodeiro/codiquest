// CodiQuest
// To prevent mocks from appearing in the final version, when building,
// it is set the prerenter const to false and add the "strict: false"
// to the adapter in svelte.config.js

export const prerender = true;
